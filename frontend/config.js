// The frontend is served by the same Node service as the API (Coolify/Hetzner),
// so API calls are same-origin. Leave this empty. (Only set a full URL if the
// frontend is ever hosted separately from the backend again.)
window.API_BASE = '';
